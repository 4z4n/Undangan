// Menambahkan efek interaktif saat halaman dimuat
window.addEventListener('load', function() {
    const rsvpButton = document.querySelector('.rsvp-button');
    rsvpButton.classList.add('rsvp-appear');
});
